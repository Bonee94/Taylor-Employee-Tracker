const inquirer = require("inquirer");
const dbQuery = require("./db_queries");
const timedPrompt = require("./timed_prompt");

const fullName = (first, last) => {
  return first + " " + last;
};


const salariesByDept = async () => {
  
};

module.exports = salariesByDept;